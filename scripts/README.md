# Scripts

Small, repeatable utilities for development, validation, setup and operations belong here.

Scripts should be non-interactive by default and safe to run more than once whenever practical.

