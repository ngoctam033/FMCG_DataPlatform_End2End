# Local data

This directory is reserved for local development data. Dataset contents are ignored by Git by default.

Only small, anonymized, explicitly redistributable fixtures may be committed. Test fixtures should normally be placed under `tests/fixtures/`.

