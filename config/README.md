# Configuration

Version-controlled, non-secret configuration shared by platform components belongs here.

Environment-specific credentials and sensitive values must be supplied through environment variables or a secret manager and must not be committed.

